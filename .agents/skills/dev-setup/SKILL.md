---
name: dev-setup
description: Set up and diagnose a Morphe Android patch-development environment. Use for first-time setup, dependency installation, Gradle package authentication, repository cloning, or IDE/build environment failures.
---

# dev-setup

## Purpose

Set up and diagnose a Morphe Android patch-development environment.

## Triggers

Use for first-time setup, dependency installation, Gradle package authentication, repository cloning, or IDE/build environment failures.

## Inputs

Operating environment; repository locations; available JDK and Android tools; authentication state.

## Outputs

Working toolchain and verified build setup, or a precise missing-prerequisite report.

## Instructions

Read [Kiro reference](references/kiro-skill.md) completely before acting. Treat its YAML frontmatter as archived metadata; follow its Markdown body as skill guidance. Preserve exact technical names, commands, code templates, fingerprint rules, and failure handling. Adapt unavailable tool names to equivalent runtime capabilities. Never weaken Smali verification or repository safety rules.