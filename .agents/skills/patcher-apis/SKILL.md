---
name: patcher-apis
description: Apply advanced Morphe patcher and BytecodeUtils APIs for bytecode navigation and mutation. Use for instruction search, return or literal overrides, labels, view hiding, hierarchy traversal, resource operations, or bulk string replacement.
---

# patcher-apis

## Purpose

Apply advanced Morphe patcher and BytecodeUtils APIs for bytecode navigation and mutation.

## Triggers

Use for instruction search, return or literal overrides, labels, view hiding, hierarchy traversal, resource operations, or bulk string replacement.

## Inputs

Mutable method/class/resource target and required transformation.

## Outputs

Correct API calls and safe mutation sequence.

## Instructions

Read [Kiro reference](references/kiro-skill.md) completely before acting. Treat its YAML frontmatter as archived metadata; follow its Markdown body as skill guidance. Preserve exact technical names, commands, code templates, fingerprint rules, and failure handling. Adapt unavailable tool names to equivalent runtime capabilities. Never weaken Smali verification or repository safety rules.