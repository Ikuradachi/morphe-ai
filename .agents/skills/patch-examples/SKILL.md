---
name: patch-examples
description: Choose and adapt proven Morphe patch patterns from real applications. Use while writing patches that need method overrides, list filtering, chained fingerprints, dynamic matching, injection, or string replacement.
---

# patch-examples

## Purpose

Choose and adapt proven Morphe patch patterns from real applications.

## Triggers

Use while writing patches that need method overrides, list filtering, chained fingerprints, dynamic matching, injection, or string replacement.

## Inputs

Verified target behavior, method shape, Smali evidence, and desired patch effect.

## Outputs

Selected minimal pattern plus adapted Kotlin patch approach.

## Instructions

Read [Kiro reference](references/kiro-skill.md) completely before acting. Treat its YAML frontmatter as archived metadata; follow its Markdown body as skill guidance. Preserve exact technical names, commands, code templates, fingerprint rules, and failure handling. Adapt unavailable tool names to equivalent runtime capabilities. Never weaken Smali verification or repository safety rules.